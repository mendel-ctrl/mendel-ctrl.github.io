# Local design/source assets — the deployed page uses an inline (base64)
# hero image, so these originals aren't needed in the public repo.
*.jpg
*.jpeg
*.JPG
*.png
Screenshot*
# Internal notes, kept local (not published to the site repo).
AUTOMATION.md
# Working content drafts (RTF source pastes), kept local.
*.rtf

# VM auto-updater (runs on your Google Cloud VM, not part of the website).
vm-automation/

# Editor/session
.claude/
